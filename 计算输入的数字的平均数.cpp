#include <stdio.h>

int main (void)
{
    int average, num;
    double sum;

    average = 0;
    sum = 0;

    printf("Enter a number: ");
    scanf("%d", &num);

    while(x > 0){
        sum += num;
        average++;
        printf("Enter a number: ");
        scanf("%d", &num);
    }
    printf("%f\n", sum / average;

    return 0;
}